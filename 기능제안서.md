# 영차영차 투자일기 — 신규 기능 기획서

## 앱 현황
국내/해외 거래 입력 → 엑셀 내보내기/불러오기 → 해외 양도세 계산까지 갖춘 1인 투자 기록 도구.
Chart.js/react-chartjs-2가 이미 설치되어 있으나 미사용 상태이며, localStorage 기반의 완전 클라이언트 앱이므로 백엔드 없이 구현 가능한 기능이 넓다.

기능 제안은 **UX 완성도 / 분석·인사이트 / 세금·리포트 / 사용자 경험 혁신** 네 축으로 분류했다.

---

## 1. UX 완성도 — 지금 있는 기능을 더 잘 쓰게

### 1-1. 거래 내역 인라인 수정 (Edit-in-place)
- **문제**: 현재 잘못 입력하면 삭제 후 재입력 필요
- **기능**: 테이블 행 클릭 시 인라인 편집 또는 모달 편집 팝업
- **구현 포인트**: `EntryTable.tsx` → 편집 모드 state, `useDashboard.ts` → `updateEntry()` 액션 추가

### 1-2. 삭제 확인 & 실행취소 (Undo)
- **문제**: 삭제가 즉시 반영되고 복구 불가
- **기능**: 삭제 후 5초 동안 토스트에 [취소] 버튼 제공, 그 사이에 `entries` 복원 가능
- **구현 포인트**: 삭제 대신 `pendingDelete` state → 타이머 만료 후 실제 삭제

### 1-3. 날짜 범위 필터 & 종목 검색
- **문제**: 거래 수가 많아질수록 탐색이 어려움
- **기능**: 테이블 상단에 "시작일 ~ 종료일" 필터 + 종목명 검색창
- **구현 포인트**: `EntryTable.tsx` 상단에 필터 UI, `useMemo`로 파생 상태

### 1-4. 페이지네이션 or 가상 스크롤
- **문제**: 1,000건 이상이면 테이블이 무거워짐
- **기능**: 페이지 당 50건 + 페이지 네비게이션 (또는 react-window 가상 스크롤)
- **구현 포인트**: `EntryTable.tsx` 내부에서 slice

### 1-5. 단축키 & 빠른 입력 모드
- **기능**: `N` 키로 폼 포커스, `Enter`로 종목→수량→단가 순서대로 이동, `Ctrl+S`로 저장
- **구현 포인트**: `useEffect`에서 keydown 리스너 등록

---

## 2. 분석·인사이트 — Chart.js를 드디어 활용

### 2-1. ⭐ 포트폴리오 현황 (보유잔고) 화면  [최우선]
- **기능**: 현재 보유 종목 목록, 평균단가, 보유수량, 평가금액 (현재가 수동 입력)
- **계산**: 매수 누적 − 매도 누적 = 잔여 수량 × 평균단가
- **구현 포인트**: `calculations.ts`에 `calcHoldings()` 추가, 새 탭으로 표시

### 2-2. ⭐ 수익률 차트 (월별/연도별 P&L)
- **기능**: 월별 실현손익 막대그래프 + 누적 수익 꺾은선 그래프
- **구현 포인트**: `react-chartjs-2` 이미 설치됨, 데이터는 매도 내역을 월별로 집계

### 2-3. 종목별 수익률 랭킹
- **기능**: 실현손익 기준 TOP 5 / BOTTOM 5 종목 리스트
- **데이터**: 매도 완료된 포지션에서 종목별 P&L 합산

### 2-4. 거래 유형별 파이 차트
- **기능**: 매수/매도/배당/입출금 금액 비율을 도넛 차트로 시각화

### 2-5. 거래 통계 요약 카드

| 지표 | 설명 |
|------|------|
| 승률 | 이익 실현 매도 / 전체 매도 × 100 |
| 평균 수익률 | 종목별 P&L % 평균 |
| 최대 낙폭 | 단일 거래 최대 손실액 |
| 총 수수료 | 전체 fee 합산 |
| 총 배당금 | div 타입 settlement 합산 |

### 2-6. 자산 흐름 (입출금 vs 수익 분리)
- **기능**: "내가 넣은 돈" vs "투자 수익으로 늘어난 돈" 분리 표시
- **계산**: 순입금 (deposit − withdraw) vs 실현 P&L 합계

### 2-7. 연도별 성과 비교
- **기능**: 연도별 수익률/거래량 비교 바 차트

---

## 3. 세금·리포트 — 납세 지원 강화

### 3-1. 국내 주식 양도세 계산기
- **기능**: 국내 대주주 기준(10억 이상 or 지분 1% 이상) 양도세 시뮬레이션
- **계산**: 중소기업 20%, 대주주 25% 구분, 기본공제 250만 원
- **구현 포인트**: `calculations.ts`에 `calcDomesticCapitalGains()` 추가

### 3-2. 배당세 리포트
- **기능**: 연도별 배당금 합산 + 원천징수 15.4% 확인, 종합소득세 합산 여부 안내
- **데이터**: `type === 'div'` 필터 후 연도별 집계

### 3-3. 세금 신고용 Excel 내보내기
- **기능**: 양도소득세 신고용 레포트 (종목명, 매수단가, 매도단가, 수량, P&L) Excel 출력
- **구현 포인트**: `excel.ts`에 `exportTaxReport()` 함수 추가, 별도 시트 구성

### 3-4. 세금 납부 기한 알림 배너
- **기능**: "해외 양도세 신고 기한: 5월 31일까지" 알림 배너 (4~5월에만 자동 표시)
- **구현 포인트**: 현재 날짜 기준으로 4월~5월 사이에 배너 조건부 노출

### 3-5. FIFO / 가중평균 원가법 선택
- **기능**: 원가법 선택 드롭다운 (현재 가중평균 고정 → FIFO 선택 가능)
- **구현 포인트**: `calculations.ts`에 `costMethod: 'avg' | 'fifo'` 파라미터 추가

---

## 4. 사용자 경험 혁신 — 일기장답게

### 4-1. ⭐ 투자 메모 / 매매 일지
- **기능**: 종목별 또는 날짜별 자유 텍스트 메모 작성 (매매 이유, 시황 기록)
- **데이터**: `{ id, date, stock?, memo }[]` 를 localStorage에 별도 저장
- **UI**: 테이블 행 우측에 메모 아이콘, 클릭 시 슬라이드 패널

### 4-2. 목표 수익률 / 손절 라인 설정
- **기능**: 종목별 목표가와 손절가 설정 → 현재가 입력 시 달성 여부 표시
- **UI**: 보유잔고 화면에 진행바 (손절가 ← 현재가 → 목표가)

### 4-3. DCA (정기매수) 시뮬레이터
- **기능**: "매달 X만 원씩 Y 종목을 Z년 동안 매수하면?" 시뮬레이션
- **구현**: 입력 폼 + 계산 결과 테이블 (누적투자금, 예상 주수, 평균단가)

### 4-4. 환율 히스토리 & 자동 환율
- **기능**: 과거에 적용한 환율 기록 보관 (거래일자 + 환율 쌍)
- **확장**: 공개 환율 API 연동하여 자동 입력 버튼 제공

### 4-5. 다크 모드
- **기능**: 시스템 다크모드 자동 감지 or 수동 토글
- **구현**: `globals.css` CSS 변수화 후 `@media (prefers-color-scheme: dark)` 추가

### 4-6. 다중 포트폴리오 (계좌 구분)
- **기능**: "미래에셋" / "키움" 등 계좌별 데이터 분리
- **구현**: `Entry`에 `account: string` 필드 추가, 헤더에 계좌 선택 드롭다운

### 4-7. PWA (Progressive Web App) 지원
- **기능**: 홈 화면 설치, 오프라인 동작, 모바일 최적화
- **구현**: `next-pwa` 패키지 + `manifest.json` + Service Worker

### 4-8. 데이터 백업 & 복원 (JSON)
- **기능**: 엑셀 외에 JSON 형식으로도 내보내기/불러오기 (메모, 설정까지 포함한 완전한 백업)
- **구현**: `JSON.stringify(allData)` → Blob 다운로드

### 4-9. 감정 태그 / 매매 이유 분류 (투자 심리 추적)
- **기능**: 거래 입력 시 매매 이유 태그 선택
  - 📈 추세추종 / 📊 실적기대 / 😱 공포매수 / 💡 분석 / 🎲 감으로 등
- **분석**: 태그별 평균 수익률 → "공포매수 시 수익률이 가장 높았음" 같은 인사이트 도출

### 4-10. 거래 패턴 분석 히트맵
- **기능**: 요일별/월별 거래 빈도 히트맵 (GitHub 잔디 스타일)
- **인사이트**: "월요일에 매수한 종목의 평균 수익률", "월별 거래 금액 추이"

---

## 우선순위 요약

| 순위 | 기능 | 난이도 | 임팩트 |
|------|------|--------|--------|
| 1 | 보유잔고(포트폴리오) 화면 | 중 | 매우 높음 |
| 2 | 월별 P&L 차트 (Chart.js 활용) | 낮 | 높음 |
| 3 | 거래 인라인 수정 | 낮 | 높음 |
| 4 | 투자 메모/매매 일지 | 중 | 높음 |
| 5 | 삭제 실행취소(Undo) | 낮 | 중 |
| 6 | 날짜 범위 필터 + 검색 | 낮 | 중 |
| 7 | 거래 통계 카드 (승률 등) | 낮 | 중 |
| 8 | 세금 리포트 Excel 내보내기 | 중 | 중 |
| 9 | 다크 모드 | 낮 | 중 |
| 10 | PWA 지원 | 중 | 높음 |
| 11 | 감정 태그 / 매매 이유 | 중 | 독창적 |
| 12 | DCA 시뮬레이터 | 중 | 중 |
| 13 | 다중 포트폴리오 | 높음 | 높음 |
| 14 | FIFO 원가법 선택 | 중 | 중 |
| 15 | 거래 패턴 히트맵 | 중 | 독창적 |

---

## 바로 구현 가능한 퀵윈 (코드 변경 최소)

1. **거래 통계 카드** — 기존 `entries` 필터링으로 승률·수수료 합계·배당 합계 즉시 표시
2. **월별 P&L 막대 차트** — `react-chartjs-2` 이미 설치됨, 데이터만 집계하면 됨
3. **삭제 Undo 토스트** — 기존 토스트 시스템 재활용, 5초 타이머만 추가
4. **종목 검색 필터** — `EntryTable.tsx`에 input 하나 + `useMemo` filter
5. **다크 모드** — CSS 변수화 후 미디어쿼리 추가
