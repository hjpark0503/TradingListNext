# 영차영차 주식 거래일지 — UI/UX 개선 제안서

> 목표: 토스 스타일 — 불필요한 요소 제거, 핵심 수치 강조, 직관적 인터랙션

---

## 목차

1. [현재 화면 구조 요약](#1-현재-화면-구조-요약)
2. [개선 우선순위 한눈에 보기](#2-개선-우선순위-한눈에-보기)
3. [영역별 상세 개선안](#3-영역별-상세-개선안)
4. [신규 기능 제안](#4-신규-기능-제안)
5. [모바일 반응형 개선](#5-모바일-반응형-개선)
6. [색상·타이포그래피 미세조정](#6-색상타이포그래피-미세조정)

---

## 1. 현재 화면 구조 요약

```
Header (로고 | 국내/해외 토글 | 다크모드·불러오기·내보내기)
  │
  ├── FAB 버튼 (하단좌) → 거래 입력 폼 사이드바 (열렸을 때 2열 레이아웃)
  │
  └── Dashboard
        ├── 서머리 카드 행 1: [실현손익 2칸] [원금 2칸] [빈칸 1칸]
        ├── 서머리 카드 행 2: [매수] [매도] [입금] [출금] [배당금]
        ├── 보유 종목 행:    [도넛 차트 2fr] [보유 종목 내역 3fr]
        ├── 실현손익 내역 패널 (일자별 / 종목별 바 차트 + 아코디언)
        ├── 거래 내역 탭 테이블 (전체·매수·매도·입금·출금·배당금)
        └── 양도세 계산기 (해외 전용)
```

---

## 2. 개선 우선순위 한눈에 보기

| 우선순위 | 영역 | 문제 | 개선 방향 |
|:---:|---|---|---|
| 🔴 HIGH | 서머리 카드 | 주요 지표가 "건수"로 표시됨 | 금액을 대표값으로, 건수는 보조 |
| 🔴 HIGH | 거래 입력 FAB | 숨겨진 진입점 — 첫 방문자가 어떻게 입력하는지 모름 | 빈 상태일 때 CTA 유도 화면 제공 |
| 🔴 HIGH | 카드 클릭 연결 | 카드를 클릭하면 아래 탭이 바뀌는데 시각적 연결 없음 | 스크롤 + 하이라이트 애니메이션 |
| 🟠 MED | 거래 테이블 | 9개 컬럼 동시 노출 — 정보 과부하 | 기본 컬럼 축소, "더보기" 컬럼 추가 |
| 🟠 MED | 폼 내 마켓 중복 | 헤더 + 폼 안에 국내/해외 토글이 2번 | 헤더 토글만 유지, 폼에서 제거 |
| 🟠 MED | 삭제 UX | 5단계 삭제 플로우 (FAB→선택→체크→쓰레기통→확인) | 행 스와이프 or 편집 모달 내 삭제 단순화 |
| 🟡 LOW | 원금 카드 위치 | 5열 그리드에서 3번째 열이 비어 있음 | 그리드 재배치 |
| 🟡 LOW | 빈 상태 | 각 탭의 "내역이 없습니다." 텍스트만 있음 | 일러스트 or 아이콘 + 안내 문구 |
| 🟡 LOW | 수익률 지표 없음 | 실현손익 금액만 있고 수익률(%) 없음 | 카드에 % 추가 |

---

## 3. 영역별 상세 개선안

### 3-A. 헤더

**현재 문제:**
- 앱 이름 "영차영차 주식 거래일지"가 `.header h1`에 있는데 폰트가 1.0rem으로 너무 작아 존재감이 없음
- 버전 뱃지(`v1.0.5`)가 헤더에 노출되어 있어 사용자에게 불필요한 정보

**개선 제안:**

```
[로고 아이콘 + 앱 이름]  |  [🇰🇷 국내 / 🌐 해외]  |  [불러오기] [내보내기] [☀/🌙]
```

- 앱 이름 폰트를 1.1rem, font-weight 800으로 강조
- 버전 뱃지는 헤더에서 제거 (푸터 또는 설정 화면으로 이동)
- `불러오기` / `내보내기` 버튼 텍스트는 현재도 모바일에서 숨김 처리(`btn-text`)되어 있는데, 데스크탑에서도 아이콘+텍스트 조합이 적합한지 검토 필요

---

### 3-B. 서머리 카드 그리드 — 가장 중요한 개선

**현재 문제:**
```
[실현손익 2칸]  [원금 2칸]  [빈칸]
[매수: 5건]    [매도: 3건]  [입금: 2건]  [출금: 0건]  [배당금: 1건]
```
- 매수/매도 카드의 핵심 표시 값이 "5건"(건수) — 사용자가 궁금한 건 금액임
- 3번째 빈칸이 레이아웃 허전함을 만듦
- 실현손익과 원금을 제외한 카드가 클릭하면 아래 탭이 이동하는데, 이 동작이 직관적이지 않음

**개선 제안:**

```
[실현손익]    [원금]       [수익률]
[매수 총액]   [매도 총액]  [입금]  [출금]  [배당금]
```

- **실현손익 카드:** 현재 금액 + 우측 하단에 수익률(%) 추가
  ```
  실현손익
  +₩1,240,000
  가중평균 기준  |  +8.3%
  ```
- **매수/매도 카드:** 금액을 primary value, 건수를 note로 이동
  ```
  매수
  ₩12,500,000   ← 총 정산금액
  25건           ← note 위치
  ```
- **수익률 카드 추가 (신규):** `(실현손익 / 총 매수 정산금액) × 100`
- **빈칸 제거:** 5열 → 두 번째 행만 5개로, 첫 행은 3열로 재편

---

### 3-C. 거래 입력 FAB & 폼

**현재 문제:**
- FAB 버튼이 하단 좌측에 있어 처음 방문한 사용자가 "어디서 입력하나?" 못 찾을 수 있음
- 폼 내부에 국내/해외 토글이 있어 헤더와 중복 (2곳에서 같은 state를 제어)
- "초기화" 버튼이 "내역 추가" 버튼 바로 왼쪽 → 실수로 클릭 위험

**개선 제안:**

**① 빈 상태일 때 CTA 화면 추가**
```
────────────────────────────────────
        📋

  아직 거래 내역이 없어요
  거래일지를 시작해볼까요?

  [✏  거래 입력하기]   [📂 엑셀 불러오기]
────────────────────────────────────
```

**② 폼 내 마켓 토글 제거**
- 헤더의 마켓 토글 1곳만 유지
- 폼 상단에 현재 마켓을 텍스트로만 표시 (클릭 불가 뱃지): `🇰🇷 국내 거래`

**③ 초기화 버튼 이동**
- "초기화"를 폼 상단 오른쪽 작은 텍스트 링크로 이동, 주 CTA와 분리
- `[거래 입력하기]` 버튼은 full-width로 강조

**④ 폼 입력 UX 개선**
- 거래유형 선택 후 불필요한 필드가 `opacity: 0.35`로 흐려지는데, 완전히 숨기는 편이 더 깔끔함 (현재 `display:none`은 수수료 필드에만 적용됨)
- "정산금액" 자동계산 박스를 더 눈에 띄게 강조 — 현재는 배경색만 있음

---

### 3-D. 보유 종목 영역

**현재 문제:**
- `현재가 조회` 버튼을 수동으로 눌러야 함 (자동 fetch가 있지만 첫 로드에만 동작)
- 버튼 tooltip에 "국내 종목은 코드 기준 조회 필요"가 있는데, 사용자에게 무슨 뜻인지 불명확
- 도넛 차트가 왼쪽 2fr, 보유 목록이 오른쪽 3fr인데, 보유 종목이 없을 때 도넛 차트가 비어 보임
- 360px 고정 높이 — 종목이 많으면 스크롤, 없으면 허전

**개선 제안:**

**① 현재가 조회 상태를 더 명확하게:**
```
보유 종목 내역
종목 없음일 때: 빈 상태 메시지
조회 중: 스켈레톤 UI (각 행에 shimmer animation)
조회 완료: 현재가 + 등락률 표시 + "5분 전 업데이트" 타임스탬프
```

**② 평가금액 컬럼 추가:**
- 현재: 평균단가 | 현재가 | 평가손익
- 개선: 평균단가 | 현재가 | **평가금액** | 평가손익(%)

**③ 높이 고정 제거:**
- 보유 종목 수에 따라 높이가 자동으로 늘어나도록 (`height: auto`, `max-height: 480px`)

---

### 3-E. 거래 내역 테이블

**현재 문제:**
- 9개 컬럼(거래일, 종목명, 거래유형, 단가, 수량, 거래금액, 수수료, 세금, 정산금액)이 한 번에 노출
- 수수료/세금 컬럼은 0인 경우가 많아 공간 낭비
- 컬럼 정렬 기능 없음
- 검색/필터 없음 — 종목명, 날짜 범위 등 필터 불가
- 행 클릭이 편집 모달을 여는 건데 이 사실이 UX상 명시되지 않음 (클릭 커서 외 힌트 없음)
- `상세내용(detail)` 필드가 Entry 모델에 있는데 테이블에 표시되지 않음

**개선 제안:**

**① 기본 컬럼 축소:**
```
거래일  |  종목명  |  거래유형  |  수량  |  정산금액  |  (…더보기)
```
- 단가, 거래금액, 수수료, 세금은 "더보기" 토글 또는 행 확장으로 이동
- 정산금액을 가장 오른쪽 강조 컬럼으로 유지

**② 컬럼 정렬:**
- 거래일, 정산금액 컬럼 헤더 클릭 시 정렬

**③ 검색 바 추가:**
- 탭 바 위에 검색 입력 (종목명 키워드, 날짜 범위 선택)
  ```
  [🔍 종목 검색...] [날짜: 2025.01.01 ~ 2025.12.31] [초기화]
  ```

**④ 행 편집 힌트:**
- 마우스 오버 시 행 우측에 `✏ 수정` 아이콘 표시
- 혹은 행 내 작은 점 3개 아이콘(`⋯`)으로 액션 메뉴 제공

**⑤ 상세내용 표시:**
- `detail`이 있는 경우 종목명 아래 작은 텍스트로 표시
  ```
  삼성전자
  2분기 실적 기대 매수
  ```

---

### 3-F. 실현손익 내역 패널

**현재 문제:**
- 차트(55%)와 아코디언 목록(45%)이 고정 비율 320px 내에 나란히 있어 좁음
- 일자별/종목별만 있고, 월별·연도별 집계 없음 (양도세 계산기에는 연도 필터가 있지만 이 패널에는 없음)
- 푸터의 총 실현손익이 1.35rem으로 너무 작음 — 가장 중요한 수치인데 강조 부족
- "⚠ 매수 데이터 부분 누락" 경고가 실현손익 카드 note에 텍스트로만 있음

**개선 제안:**

**① 탭 추가:**
```
[일자별] [월별] [종목별]
```
- 월별 탭: X축이 월(1월~12월), 연도 선택 드롭다운 제공

**② 푸터 KPI 강조:**
```
총 실현손익
+₩1,240,000   ←── 2.0rem, font-weight 800
가중평균 원가 기준

           매도 25건  |  수익 종목 8/10  |  승률 80%
```

**③ 누락 경고 UX:**
- 현재: 카드 note에 작은 텍스트 "⚠ 매수 데이터 부분 누락"
- 개선: 패널 상단에 노란 인라인 알림 배너로 표시
  ```
  ⚠  일부 종목의 매수 내역이 없어 손익이 정확하지 않을 수 있어요.
     [매수 내역 입력하기 →]
  ```

---

### 3-G. 양도세 계산기 (해외 전용)

**현재 문제:**
- 환율 입력이 양도세 계산기 섹션 안에 있어 찾기 어려움
- 연도 선택이 `<select>`인데 다른 UI 요소들(세그먼트 컨트롤)과 스타일 통일 필요
- 계산 결과 요약(공제 전 손익, 기본공제, 세율, 예상 세액)이 잘 구성되어 있음 — 유지

**개선 제안:**

**① 환율 입력 위치 이동:**
- 헤더 우측 또는 해외 마켓 선택 시 상단에 고정 표시
  ```
  💱 환율  $1 = ₩ [1,380]  (직접 입력)
  ```

**② 연도 선택 UI:**
- `<select>` → 세그먼트 컨트롤 또는 좌우 화살표 내비게이션(`← 2024 →`)으로 통일

---

### 3-H. 삭제 플로우 단순화

**현재 플로우 (5단계):**
1. 탭 바의 쓰레기통 아이콘 클릭
2. 선택 모드 진입
3. 체크박스 선택
4. 쓰레기통 클릭
5. 삭제 확인 다이얼로그에서 "삭제" 클릭

**개선 제안 (2단계):**
- 편집 모달 하단의 "삭제" 버튼으로 단일 항목 삭제 (현재도 있음 — 유지)
- 다중 삭제는 테이블 좌측 체크박스 + 상단 플로팅 액션 바:
  ```
  [✓ 3개 선택됨]  [삭제]  [취소]  ← 테이블 위에 플로팅
  ```
- 쓰레기통 FAB 단계를 없애고 바로 체크박스 모드로 진입

---

## 4. 신규 기능 제안

### 4-1. 종목별 필터 (빠른 구현)

탭 바 아래에 종목 칩(chip) 필터:
```
전체  삼성전자  SK하이닉스  카카오  …더보기
```
클릭하면 해당 종목 거래만 표시.

### 4-2. 수익률 카드

```
총 수익률
+8.3%
실현손익 / 총 매수금 기준
```
`calcRealizedPL`에서 `totalBuyCost`를 이미 계산하므로 추가 구현 비용 낮음.

### 4-3. 연간 요약 리포트 (연말 기능)

- 연도 선택 → 해당 연도 전체 실현손익, 매수/매도 횟수, 승률 요약 카드
- 양도세 계산기와 연계

### 4-4. 상세내용(메모) 검색

현재 `detail` 필드가 입력은 되지만 테이블에 표시 안 됨.
- 테이블 종목명 셀 아래 서브텍스트로 표시
- 검색 바에서 메모 내용으로 검색 가능하도록

### 4-5. 엑셀 내보내기 템플릿 선택

- 현재: 전체 컬럼 일괄 내보내기
- 개선: "간편 보기 (거래일·종목·유형·정산금액)" vs "전체 내보내기" 선택

---

## 5. 모바일 반응형 개선

**현재 브레이크포인트:**
- `≤1100px`: aside-w를 290px로 줄임
- `≤860px`: 1열 레이아웃, holdings-row 세로 분리
- `≤600px`: 텍스트 숨김, 탭 폰트 축소

**문제점:**
- 860px 이하에서 서머리 그리드가 2열로 바뀌는데, 5개 카드가 홀수라 마지막 카드가 반 칸 차지
- 600px 이하에서 거래 테이블의 9개 컬럼이 가로 스크롤 처리되어 UX가 좋지 않음

**개선 제안:**

**① 모바일 카드 그리드:**
```
≤600px:
[실현손익 전체 너비]
[매수] [매도]
[입금] [출금] [배당금]
```

**② 모바일 테이블:**
- 600px 이하에서는 테이블 대신 카드 리스트 형태로 전환:
  ```
  ┌──────────────────────────┐
  │ 2025.03.15  [매수]       │
  │ 삼성전자                 │
  │ 정산금액  ₩1,250,000    │
  └──────────────────────────┘
  ```

**③ 하단 네비게이션 바 (모바일):**
- 현재 FAB 버튼 대신 하단 고정 탭 바:
  ```
  [📊 대시보드] [📝 거래입력] [📁 내역] [⚙ 설정]
  ```

---

## 6. 색상·타이포그래피 미세조정

현재 디자인 토큰은 토스 스타일에 매우 가깝습니다. 아래 몇 가지만 조정하면 완성도가 높아집니다.

### 6-1. 손익 색상 일관성

현재 코드에서:
```css
.card-pl .pl-pos { color: var(--loss) !important; }  /* 이익 = 빨간색 */
.card-pl .pl-neg { color: var(--gain) !important; }  /* 손실 = 파란색 */
```
한국 주식 관습(이익=빨강, 손실=파랑)을 따르고 있음 — **유지**.
다만 클래스명이 `pl-pos`=빨강, `pl-neg`=파랑인데 `--loss`/`--gain` 변수명과 혼용되어 코드 가독성이 낮음. 변수명 정리 권장 (기능 변경 아님).

### 6-2. 카드 hover 강조

현재 hover 시 shadow만 진해짐. 토스는 hover 시 미세한 scale-up:
```css
.card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);  /* 추가 */
}
```

### 6-3. 폰트 사이즈 계층 정리

현재 여러 곳에서 rem 값이 `.68rem`, `.72rem`, `.75rem`, `.78rem`, `.80rem`, `.825rem`, `.875rem`으로 너무 세분화됨.
토스 스타일 권장 계층:
```
12px (0.75rem) — 캡션, 뱃지
13px (0.8125rem) — 보조 텍스트
14px (0.875rem) — 본문, 테이블 셀
16px (1.0rem) — 서브 타이틀
20px (1.25rem) — 카드 값
24px (1.5rem) — 핵심 KPI
```

### 6-4. 빈 상태 아이콘

현재 빈 상태는 텍스트만:
```
내역이 없습니다.
```
개선:
```
     📭
내역이 없어요
거래 직접 입력 버튼으로 내역을 추가해보세요.
```
SVG 아이콘 사용 권장 (이모지 대신 일관성 있는 아이콘 세트).

---

## 요약

| 분류 | 작업 | 예상 난이도 |
|---|---|:---:|
| 카드 주요값 금액으로 변경 | `cardCount` → `cardNote` swap | 쉬움 |
| 수익률 카드 추가 | 계산 로직 이미 있음 | 쉬움 |
| 폼 내 마켓 토글 제거 | props 제거 | 쉬움 |
| 빈 상태 CTA 화면 | 조건부 렌더링 추가 | 쉬움 |
| 상세내용 테이블 표시 | 테이블 셀 추가 | 쉬움 |
| 컬럼 기본 숨김/더보기 | 상태 추가 + 헤더 토글 | 보통 |
| 종목별 칩 필터 | 필터 상태 + EntryTable 수정 | 보통 |
| 검색 바 | 입력 상태 + 필터 로직 | 보통 |
| 월별 실현손익 탭 | calcPLByMonth 함수 추가 | 보통 |
| 모바일 카드 리스트 뷰 | CSS + 조건부 렌더링 | 어려움 |
| 하단 네비 바 (모바일) | 레이아웃 구조 변경 | 어려움 |
